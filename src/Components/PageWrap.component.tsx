import { makeStyles } from '@material-ui/styles';
import React from 'react';


const useStyles = makeStyles({

});



export const PageWrap = (props: any) => {

    const styles = useStyles();

    return (
        <div >

        </div>
    )

}